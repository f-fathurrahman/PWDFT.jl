function test_main()
    Npoints = 100
    MIXDIM = 4
    X = zeros(Npoints,MIXDIM)
    F = zeros(Npoints,MIXDIM)

    for i = 1:5
        Rhoe_new = rand(Npoints)
        Rhoe = rand(Npoints)
    end
end